me: Philip sees where a decision, information or follow-up is required.']];
    cards.forEach((c,i)=>{if(cv[i])c.innerHTML=`<div class="metric-index overview-index">${i+1}</div><h3>${cv[i][0]}</h3><p>${cv[i][1]}</p><div class="result">${cv[i][2]}</div>`;});
    html('#panel-overview .callout','<strong>Why Egor is needed instead of simply using ChatGPT.</strong> A standalone AI tool can draft text. I connect demand discovery, qualification, engineering review, CRM and performance control into one repeatable process — and take responsibility for operating it.');
    const proofs=$$('#panel-overview .proof-card');
    if(proofs[0])proofs[0].innerHTML='<span class="badge">Example 1 · Demand signal</span><h3>Identify a procurement window in time</h3><p>A public signal becomes a sales decision card within hours: relevance, unknown requirements, owner, deadline and next action.</p><div class="result">Output: qualify now, monitor or decline.</div>';
    if(proofs[1])proofs[1].innerHTML='<span class="badge">Example 2 · RFQ preparation</span><h3>Collect missing inputs before promising a machine</h3><p>The customer request becomes a structured brief. Missing data is exposed and the final technical decision remains with the engineer.</p><div class="result">Output: reach a meaningful engineering discussion faster.</div>';
    const roles=$$('#panel-overview .role-card');
    if(roles[0])roles[0].innerHTML='<span class="badge">Egor</span><h3>Owns operation of the commercial process</h3><ul><li>find and qualify demand signals;</li><li>prepare materials for sales and engineering;</li><li>define validation rules and next actions;</li><li>monitor data quality and deal movement;</li><li>report against the agreed metric.</li></ul>';
    if(roles[1])roles[1].innerHTML='<span class="badge">STOCH</span><h3>Retains the final technical and commercial decision</h3><ul><li>equipment applicability;</li><li>final selection and configuration;</li><li>price, delivery time and terms;</li><li>commissioning, warranty and obligations;</li><li>customer relationship.</li></ul>';
    text('#panel-presentation .section-kicker','Decision deck'); text('#panel-presentation h2','Six slides: where growth can occur and how to validate it safely'); text('#panel-presentation .section-note','The original Russian deck is preserved for the STOCH team; English captions explain the commercial meaning of each slide.');
    const captions=['Three outcomes: more suitable opportunities, faster response and fewer stalled deals.','One process connecting demand discovery, engineering response preparation and pipeline control.','How to identify a relevant procurement opportunity in time and make a Go / No-Go decision.','How to reduce manual RFQ analysis while keeping the final technical decision with engineering.','Six questions that define the process, owner, baseline metric and continuation criteria.','The path from discussion to a 3–5 case test, prototype and working MVP.'];$$('#panel-presentation figc